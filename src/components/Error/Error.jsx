import error from '../../assets/img/error.gif';

const Error = _ => {
    return (
        <section id="error">
            <img src={error} alt="error" />
        </section>
    )
}

export default Error;