import loading from '../../assets/img/loading.gif';

const Loading = _ => {
    return (
        <section id="loading">
            <img src={loading} alt="loading" />
        </section>
    )
}

export default Loading;